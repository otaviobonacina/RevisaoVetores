import java.util.Random;
public class Vetor4op {
    public static void main(String[]args){
        Random gerador = new Random();
        int numero[]=new int [5];
        for (int i = 0; i < 5; i++) {
            System.out.println(gerador.nextInt(100));
            numero[i]=gerador.nextInt(100);
            
        }}
}
